---
layout: base.njk
title: mile 70
eleventyNavigation:
  key: Home
  order: 1
---

<h1 class="home-hero text-center mx-auto sm:max-w-[75%] font-extrabold text-2xl">
  <span class="text-green-800">m70</span> ("{{title}}") is a site to inspire those
  who want to push beyond mediocrity both mentally and physically.
</h1>

<h2 class="text-center font-semibold text-2xl my-8">Latest Post</h2>
<article class="flex items-center justify-center">
  <a href="blog/{{contentfulBlogPosts.items[0].fields.datePosted | formatDate}}/{{contentfulBlogPosts.items[0].fields.title | slug}}" class="flex flex-col space-y-5 items-start justify-center hover:bg-green-100 p-4 rounded-lg">
    <h4 class="font-sans font-bold text-lg">
      {{contentfulBlogPosts.items[0].fields.title}}</h4>

    <p class="font-sans text-sm">{{contentfulBlogPosts.items[0].fields.summary}}</p>

    <p class="font-sans font-bold text-sm">read more...</p>
  </a>
</article>