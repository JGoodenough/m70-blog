---
title: mile 70
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>

    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <header class="font-sans max-w-5xl mx-auto flex justify-between items-center px-2 pt-8 pb-4 border-b border-green-600 sticky top-0 z-50 bg-white">
      <h1 class="home font-bold text-green-800 text-2xl hover:bg-green-100 rounded-lg p-2">
        <a href="{{ '/' | url }}">{{ metadata.title }}</a>
      </h1>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      <nav>
        <ul class="flex justify-between items-center">
          {%- for entry in collections.all | eleventyNavigation %}
            <li class="nav-item{% if entry.url == page.url %} nav-item-active font-bold text-green-800{% endif %} pr-4">
              <a href="{{ entry.url | url }}" class="hover:bg-green-100 rounded-lg p-2">{{ entry.title }}</a>
            </li>
          {%- endfor %}
        </ul>
      </nav>
    </header>
    <main class="max-w-5xl mx-auto py-12 px-2">
      {{content | safe}}
    </main>
    <footer class="max-w-5xl"></footer>
  </body>
</html>