---
title: mile 70
---

<!doctype html>
<html lang="en">
  <head>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-MDP7B5MSH8"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag() {
        dataLayer.push(arguments);
      }
      gtag('js', new Date());

      gtag('config', 'G-MDP7B5MSH8');
    </script>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/favico/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/favico/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/favico/favicon-16x16.png">
    <link rel="manifest" href="/assets/favico/site.webmanifest">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&display=swap" rel="stylesheet">
    <link href="/styles.css" rel="stylesheet"/>
    <link rel="sitemap" href="/sitemap.xml"/>
  </head>
  <body>
    <header class="font-sans max-w-5xl mx-auto flex flex-col sm:flex-row justify-between items-center px-2 sm:pt-8 pb-0 border-b border-green-600 sticky top-0 z-50 bg-white">
      <div class="flex items-end justify-start">
        <h1 class="nav-logo font-bold text-green-800 text-5xl p-2 pb-0 mb-0">
          <a href="{{ '/' | url }}">{{ metadata.title }}</a>
        </h1>
        <p class="nav-logo__sub-line pl-2 pb-2 mt-0">stories that push beyond limits</p>
      </div>

      {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
      <nav>
        <ul class="nav-links flex justify-between items-center my-4">
          {%- for entry in collections.all | eleventyNavigation %}
            <li class="{% if entry.url == page.url %}font-extrabold text-green-800{% endif %} pr-4 font-lg">
              <a href="{{ entry.url | url }}" class="{% if entry.url == page.url %}bg-green-100 rounded-lg px-4{% endif %} hover:bg-green-100 rounded-lg px-4 py-2">{{ entry.title }}</a>
            </li>
          {%- endfor %}
        </ul>
      </nav>
    </header>
    <main class="max-w-5xl mx-auto py-12 px-8 sm:px-2">
      {{content | safe}}
    </main>
    <footer class="max-w-5xl"></footer>
    {% set supabaseCDN %}
    {% include "auth/supabase-cdn.njk" %}
    {% endset %}

    {% set signoutScript %}
    {% include "auth/supabase.njk" %}
    {% include "auth/js/signoutbtn.js" %}
    {% endset %}

    {% edge "njk", {
      supabaseCDN: supabaseCDN,
      signoutScript: signoutScript
    } %}
    {% if isUser %}
      {{ supabaseCDN | safe }}
      <script>
        {{ signoutScript | safe }}
      </script>
    {% endif %}
    {% endedge %}
  </body>
</html>